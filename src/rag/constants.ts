// Constants for batch processing
export const BATCH_SIZE = 2; // Number of files to process at once
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB max file size

// Constants for vector dimensions
export const VECTOR_DIMENSION = 768;

// Constants for text splitting
export const DEFAULT_CHUNK_SIZE = 1000;
export const DEFAULT_CHUNK_OVERLAP = 200;
export const DEFAULT_TOP_K = 4;
